import { CapacitorConfig } from '@capacitor/cli'

const config: CapacitorConfig = {
  appId: 'codes.gue.podnuxt',
  appName: 'podnuxt',
  webDir: 'dist',
  bundledWebRuntime: false,
}

export default config
